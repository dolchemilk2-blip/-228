# сборка (build.py) — нужен ещё ffmpeg в системе
numpy
soundfile
pyloudnorm
scipy            # для фильтров в clean.py; без неё чистка работает, но без вырезания гула

# чистка голоса (clean.py), ставится отдельно — тянет torch, около 3 ГБ:
# pip install "audio-separator[cpu]"   # движки deecho / deecho+ / dereverb / denoise
# pip install clearvoice               # движок speech (MossFormer2_SE_48K)

# для tools/recognize.py (по желанию):
# sherpa-onnx
